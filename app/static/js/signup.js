form = document.querySelector('form');
form.addEventListener('submit', (next) => {
    if(document.getElementById("is_creator").checked) {
        document.getElementById('is_creator_hidden').disabled = true;
        ne
    }
    console.log("ciao")
}, true);